export class Card {

    public description: string;
    public status: number;

    //comment added to cardsOnly branch  
  
    //constructor(public title: string, public status: number) {}
    constructor(public title: string) {}
}
